const MAX_PROPERTY_MEDIA = 10;
const MAX_PROPERTY_DOCUMENTS = 30;

const STORAGE_PROVIDERS = Object.freeze(['local', 'cloudinary', 'r2']);

module.exports = {
  MAX_PROPERTY_MEDIA,
  MAX_PROPERTY_DOCUMENTS,
  STORAGE_PROVIDERS,
};
